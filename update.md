# 更新日志 (Update Log)

---

# C++ 重构版 (factory-td)

## Alpha v1.3.3 (当前版本 - 2026-09-14)

### 下载（Windows 64 位 · 免安装）

- **⬇ [factory-td-v1.3.3-win64.zip](https://github.com/liuyandi386/jyfactorio/releases/download/v1.3.3/factory-td-v1.3.3-win64.zip)**
  解压后双击 `factory-td.exe` 即可游玩，**无需安装 SFML / 编译器 / 任何运行库**。
- 全部版本：[Releases 页面](https://github.com/liuyandi386/jyfactorio/releases)

> 该压缩包由 `factory-td/package.bat` 从 `build/` 产出目录自动收集打包生成（打包范围与发布流程见 [README.md](README.md) 的「打包发布（免安装绿色包）」章节）。

### 新增功能

- **品牌重塑：《异星工厂塔防》→《织星计划》（Project Weavestar）**：
  - 一句话世界观：你是「**织星工业**」的**外派工程师**，被派往一颗颗星球建厂、清障、交付指标；
    随船 AI **织女星（Vega）** 负责播报与吐槽；副标题改为「建厂 · 清障 · 交付 · 前往下一颗星球」。
  - 启动动画、主菜单标题（`W E A V E S T A R`）、窗口标题、加载界面（「正在生成星球地表」）、关于本作、退出确认文案全部换新。
  - **存档文件名（`saves/factory_td.json`）与存档格式保持不变**，旧存档可直接继续。
- **新手教程（独立模式，新增 `systems/TutorialSystem.*`）**：
  - 主菜单新增独立入口「**新手教程**」（快捷键 `T`），与「**普通关卡**」（快捷键 `N`）**平级并列、互不嵌套**；
    旧的「新手引导」开关与"新开局自动弹说明书"行为已**移除**——引导不再是"可能突然弹出来的窗口"，而是一个明确的模式选择
    （说明书本身仍在，随时按 `H` / `F1` 打开）。
  - 完整分步引导层：章节 / 步骤 / 任务判定 / 耗时统计 / 误操作计数；屏幕顶部**引导横幅 + 步骤进度 + 跳过引导按钮**；
    `F2` 引导进行中 = 跳过，引导未开始 = 重新开始。
  - 教程进度独立存 `saves/tutorial.json`，与主存档**完全隔离**：教程模式按 `F5`/`F9` 会明确提示"不保存/不读取"，
    教学过程中的临时建造**不会覆盖**玩家的普通关卡存档；普通关卡也不承载引导状态。
  - 教程模式**不自动出怪**，敌人由教学步骤手动生成（`Z`/`X`/`C`）；引导会高亮目标建筑按钮并纠正误操作（选中错误的建筑会被提示纠正）。
- **叙事层（新增 `systems/Narrative.*`）：织女星通讯 + 事件播报**：
  - 新增**右下角通讯条**（`GameUI::showComms`）：头像 + 说话人 + 台词，独立通道，不会被系统提示（Toast）挤掉。
  - 事件驱动播报：**着陆 / 第一座建筑落成 / 首次遇敌 / 首次击杀 / 一波清空 / 防护告急 / 吐槽公司**；
    波次之间播报一段简短的**开发日志**（本波战果 + 剩余生命）。
  - 同一事件的多条台词**轮换播出**，避免复读；"第一次……"这类里程碑**整局只播一次**。
  - 教程模式自带完整旁白，**不叠加**本模块（避免两套声音打架）。
- **帧率 / 垂直同步设置项**（设置界面第 4 项，高刷新率显示器适配的核心开关）：
  - 可选 **垂直同步（默认）** / 固定 60 / 固定 120 / 固定 144 / 不限制，落盘 `saves/settings.json`。
  - 应用入口全项目唯一（`gset::applyFrameMode()`），改完**立即生效**，无需重建窗口。
- **采矿场双模式（右键矿机打开设置面板）**：
  - **原有模式**（默认）：覆盖半径内所有矿点随机采集——行为与旧版完全一致。
  - **固定矿点模式**：吸附到矿点旁边，**只采所选矿种的那一个矿点**；换矿种即重新绑定/吸附，
    有限储量模式下采尽或矿点消失会自动切到覆盖范围内同矿种的其它矿点（默认无限模式矿点永不枯竭）；范围内没有该矿种时会提示并置灰对应按钮。
  - 面板提供 **模式切换 / 8 种矿石筛选 / 旋转输出面 / 关闭**，底部实时显示采集速率、覆盖半径与绑定矿点剩余储量（默认无限开采时显示 **∞**）。
  - **两种模式的产量规则完全一致**（同一个 `m.rate`、每轮结算 1 个），切换模式**不会改变产率**。
- **高 DPI 感知**：进程声明 DPI 感知，125% / 150% 缩放屏下窗口不再"只占屏幕一角"、界面不再发虚。
- **应急启动参数**：`--safe-mode` / `--windowed`（黑屏应急：强制 1280×720 窗口化 + 垂直同步，不写回设置文件）。

### Bug修复

- **机器贴图整体向左偏 90°（采矿场 / 组装机 / 发电机）**：贴图键名沿用了 Python 版"逆时针 90°"的映射 `(d+3)%4`，
  而 PNG（由 `organize_sprites.py` 顺时针生成）与程序化箭头本来就是"**后缀 == 视觉朝向**"，
  于是逻辑朝上时取到了 `left` 贴图 → 统一为"贴图后缀 == 逻辑朝向"（`machineKey` / PNG 加载路径 / 程序化箭头绘制**三处一起改**）。
- **高刷新率显示器下摄像机与移动速度翻倍**：摄像机平滑按"每帧"计算，144Hz 屏上速度约为 60Hz 的 2.4 倍
  → 改为按 `dt` 的**帧率无关**指数平滑（`CAMERA_SPEED_PER_SEC` / `CAMERA_SMOOTH_RATE`）。
- **高 DPI（125% / 150%）下无边框全屏只占屏幕一角**：改用系统报告的物理分辨率并做 DPI 感知缩放。
- **旋转输出面后"贴图箭头"与"实际输出方向"错位**：旧右键只调 `FaceConfig::rotate()`、不改建筑的 `dir`，
  导致箭头指一个方向、物品却从另一个方向出 → 新增 `MinerSystem::rotateOutputFace()`，**同步旋转 `dir` 与输出面**。
- **高刷新率下选「固定 60 帧」后画面异常/抖动**：SFML 的 `setVerticalSyncEnabled` 与 `setFramerateLimit` 会**互相拆台**
  → 收敛到 `gset::applyFrameMode()` 一处按**固定顺序**应用，并明确"两者二选一"。

### 系统优化 / 变更

- **术语去模组化**：界面与提示中的「**ME 网络 / ME 接口 / ME 终端**」统一改为「**通物网络 / 通物接口 / 通物终端**」
  （内部代码名 `Me*` 与配置键 `me_*` 保持不变，**存档与 `config.json` 完全兼容，无需迁移**）。
- 设置界面第 4 项由「新手引导」开关改为「**帧率 / 垂直同步**」；设置项定义仍由 `gset::settingRow*` 统一提供，
  启动菜单与游戏内暂停面板两处行为一致。
- `CMakeLists.txt` 纳入 `src/systems/TutorialSystem.cpp`、`src/systems/Narrative.cpp`。
- `build.bat` 构建脚本与 `Camera` / `PipeSystem` / `MeSystem` 注释同步更新。
- **矿点改回「无限开采」**：新增配置开关 **`ore_infinite`（默认 `true`）** —— 矿点默认**永不枯竭**（采多少不再扣储量、矿点不会采尽消失），
  与旧 Python 版「Factorio 风格、可无限开采」的手感完全一致；置 `false` 则恢复 v1.3.3 的有限矿点玩法（每点 1000、采尽消失）。
  采矿场设置面板的「绑定矿点剩余」在无限模式下显示为 **∞**，说明书对应文案随之变化。
- 版本号升至 **v1.3.3**（游戏内 `kVersion` 与窗口标题）。

---

## Alpha v1.3.2 (2026-09-13)

### 下载（Windows 64 位 · 免安装）

- **⬇ [factory-td-v1.3.2-win64.zip](https://github.com/liuyandi386/jyfactorio/releases/download/v1.3.2/factory-td-v1.3.2-win64.zip)**
  解压后双击 `factory-td.exe` 即可游玩，**无需安装 SFML / 编译器 / 任何运行库**。
- 全部版本：[Releases 页面](https://github.com/liuyandi386/jyfactorio/releases)

> 该压缩包由 `factory-td/package.bat` 从 `build/` 产出目录自动收集打包生成（打包范围与发布流程见 [README.md](README.md) 的「打包发布（免安装绿色包）」章节）。

### 新增功能

- **游戏内暂停面板（`ESC`）**：参考《我的世界》等游戏的暂停菜单——全屏变暗 + 居中纵向按钮列。
  - 四个按钮：**继续游戏 / 保存存档 / 设置 / 返回主界面**。
  - 键盘 ↑↓ 选择、Enter 确认、Esc 继续游戏；鼠标可点、可悬停高亮（与启动菜单一致：**鼠标真正移动时**才让悬停行接管焦点，指针停住不会夺焦）。
  - 打开时冻结世界（复用 `Game::paused`），并自动关掉可能同时打开的面板（商店/工作台/世界地图/ME/过滤/说明书/面编辑器），避免叠层。
  - **保存存档** = F5（Toast 反馈，面板保持打开）。
  - **返回主界面** 先自动保存存档，再退出本局回到标题主菜单（可重新开始新游戏或继续存档）；`main.cpp` 改为循环，回到菜单后重新进入启动入口系统。
  - **设置** 子页与启动菜单的「设置」界面**完全一致**（显示模式 / 悬停提示 / 新手引导 / 摄像机速度 / 恢复默认设置 / 返回）。
- **ESC 语义调整**：有面板或面编辑器打开时先关掉面板（MC 习惯）；什么都没有时才打开暂停面板。暂停面板打开期间键盘/鼠标全部由面板接管，不再穿透到世界。

### 系统优化

- **设置项定义抽到 `Settings`（`gset`）**：新增 `SETTING_ROW_COUNT` / `settingRowLabel()` / `settingRowValue()` / `settingRowIsAction()` / `cycleSettingRow()` / `activateSettingRow()`（含 `SettingActivate` 枚举），启动菜单设置界面与暂停面板设置子页共用同一份，**杜绝两处逻辑漂移**（"设置按钮的功能和主界面保持一致"由代码结构保证）。
- `Game` 新增 `returnToMenu` 与 `applyDisplayMode()`：暂停面板里改显示模式后，仍在**事件循环之外**重建窗口（沿用 v1.3.1"事件循环内重建 SFML 窗口会崩"的教训），重建后同步摄像机尺寸、视图与 UI 布局并保持暂停。
- `GameUI` 新增 `anyPanelOpen()`（ESC 据此决定"关面板"还是"开暂停面板"）。
- `main.cpp` 改为循环：`EntrySystem → Game →(返回主界面)→ EntrySystem`，正常关窗仍直接退出。
- 版本号升至 **v1.3.2**（游戏内 `kVersion` 与窗口标题）。

---

## Alpha v1.3.1 (2026-09-13)

### 新增功能

- **启动入口系统（`ui/EntrySystem.*`）**：把原来「main 直接 new Game」改为一套完整启动流程，结束后才按玩家选择创建游戏：
  `启动动画(工作室 logo，可跳过) → 标题界面/主菜单 → (设置界面) → 加载界面 → 创建 Game`。
  - **启动动画**：logo 淡入淡出，任意键 / 点击跳过。
  - **标题 / 主菜单**：开始新游戏 · 继续游戏（无存档时置灰）· 设置 · 关于本作 · 退出游戏；↑↓ + Enter 操作，鼠标可点可悬停；有存档时「开始新游戏」弹二次确认。
  - **加载界面为真实进度**（非假动画）：依次真正执行「读取 config.json → 校验存档 → 展开敌人路径 → 生成矿点分布 → 初始化视图」，并把**真实算出的敌人路径 + 矿点分布画成迷你地图预览**（与 Game 同用 `buildPathTiles` / 同种子，进游戏后布局一致）。
  - 背景为工业暗色氛围：渐变、滚动蓝图网格、旋转齿轮、厂房剪影 + 烟雾、上升火星。
  - 入口系统**自带独立窗口，进入游戏前即销毁**，因此 Game 可按设置重新创建窗口，两者互不干扰。
- **跨场景用户设置（`Settings.h/.cpp`，namespace `gset`）**：启动菜单与游戏内共用的四项设置，落盘 `saves/settings.json`。
  - 显示模式（窗口 1280×720 / 窗口 1600×900 / 全屏无边框·桌面分辨率）、悬停提示开关、新开局自动弹说明书开关、摄像机速度（慢 0.6 / 标准 1.0 / 快 1.6）。
  - 文件缺失或损坏 → 回落默认值不打断启动；手改坏的枚举值会被 clamp。
  - `main.cpp` 启动时先 `gset::load()`（失败则写一份默认设置）；`--selftest-save` 分支保留在入口系统之前，自检不受影响。
  - Game 的窗口尺寸/全屏按 `gset` 创建，`camera.speedScale = gset::cameraSpeedScale()`，悬停提示与「新开局自动弹说明书」分别受对应开关控制。

### Bug修复

- **鼠标悬停每帧夺取键盘焦点**：设置界面 / 主菜单的悬停命中检测写成 `if (r.contains(mp)) settingRow = i;`，导致鼠标指针停住时键盘焦点被每帧弹回该行，↑↓/←→ 形同失效（自动化截图里表现为「按了 →，显示模式没变、焦点却停在『恢复默认设置』」）→ 只有鼠标**真正移动**时才让悬停行接管键盘焦点，指针停住时仅更新高亮（记住上一帧鼠标位置做比较），保留「悬停即可选中」的手感。
- **独占全屏反复黑屏闪烁 + 鼠标漂移**：改用 `sf::Style::None` + 桌面尺寸 + 贴到 (0,0) 实现「无边框全屏」，**不调用 SetDisplayMode**，绕开独占全屏的模式切换与其导致的鼠标坐标重映射；窗口创建统一收敛到 `gset::applyToWindow()`。
- **无边框全屏露出任务栏**：`Style::None` 窗口非 topmost，会被任务栏/其它窗口盖住 → 全屏时用 `SetWindowPos(HWND_TOPMOST)` 置顶（仅 Windows），切回窗口化时窗口由 `create()` 重建、不残留置顶状态。
- **不能在 `pollEvent` 事件循环内重建 SFML 窗口**：切换显示模式需重建窗口，延后到事件循环之外（`pendingDisplayApply` 标志在 `render()` 开头统一处理），否则崩溃 / 丢事件。

### 系统优化

- 窗口创建设置策略统一到 `gset::applyToWindow()` 一处（启动菜单窗口与游戏窗口共用），避免两处各写一套导致行为不一致。
- `CMakeLists.txt` 纳入 `src/Settings.cpp`、`src/ui/EntrySystem.cpp`。
- 修正「在错误工作目录运行 exe」产生的杂散文件 `factory-td/saves/settings.json`（游戏必须在 `build/` 下运行，所有 assets/saves 路径都相对 cwd）。

---

## Alpha v1.3.0 (2026-09-09)

### 新增功能

- **游戏内说明书 / 新手引导**（TODO.md 诉求："README 没人看，建议做一个 UI 版的说明书"）：
  - **H 键 / F1** 或左上角「帮助」按钮打开全屏说明书；ESC、点击面板外部、或再按一次 H 关闭。
  - 6 个标签页：**新手引导**（7 步上手：采矿 → 运输 → 冶炼 → 加工 → 防御 → 供电 → ME 进阶）、**操作按键**、**建筑一览**（22 种建筑用途 + 造价，直接读 `BUILDING_INFOS`）、**生产与物流**、**电力系统**、**常见问题**（物品卡住/机器不工作/电网不通等排查）。
  - 内容区支持**鼠标滚轮滚动**；切换标签自动回到顶部；与商店/工作台/世界地图互斥，不会叠加。
  - **新游戏自动弹出**：没有存档（即新开局）时首次进入自动打开说明书；F5 保存过之后下次启动不再自动弹出（随时可用 H / F1 重新打开）。
- **顶部新增「帮助」按钮**（商店按钮右侧），说明书打开时按钮变绿。

### Bug修复

- **文档严重滞后于代码**：根目录 `update.md` / `ai.md` 停在 v1.2.1，而实际代码已到 v1.2.4（开发记录只写进了 `build/update.md` 副本）→ 根目录三份文档全部补齐并同步（`build/` 是构建产物目录，不作为文档源）。

---

## Alpha v1.2.4 (2026-08-25)

### 新增功能

- **商店重构 + 背包系统**：
  - 左上角新增「商店」按钮，点击进入全屏商店页（替代原有窄条商店；B 键与左上角按钮均可打开）。
  - 侧边栏改为「背包」：上方为机器格（4 列色块 + 数量，点击即选中该机器进入放置），下方为物品格；初始机器/物品各 100000。
  - 商店兑换的机器改为「成品进背包 +1」，正式放置机器时扣背包机器数（拆除返还）；移除侧边栏原有 22 个建筑选择按钮（键盘数字键仍可切建筑）。
  - 机器数接入存档（`machines` 字段，兼容旧档缺失回落到 100000）；背包格悬停显示全名 + 数量。
- **合金炉改为选定配方 + 无需电力**：
  - 合金炉不再接入电网（移除 PowerConsumer 及所有断电检查/断电红点渲染），放置即可冶炼。
  - 右键合金炉打开配方选择菜单（与组装机一致），从 4 种合金配方中选一种，之后只按该配方冶炼（原料入站/产物出站都只认当前配方）。

### Bug修复

- **初始金币/生命配置文件不生效**：`gold` / `lives` 在成员初始化列表里用了旧的 `INITIAL_GOLD` / `INITIAL_LIVES` 默认值，而 `loadConfig` 在构造体内才覆盖这两个配置变量，导致 `initial_gold` / `initial_lives` 改配置文件从不生效（恒为 500/10）→ 构造体在 `loadConfig` 之后重新取一次值。
- **矿物无法合成锭（锭被静默丢弃）**：熔炉原料与产物共用同一库存，机器拉料与 ME 出网导出两条路径持续把库存塞满，出炉时 `inv.add` 失败、锭被丢弃 → 封堵两条塞料路径、出锅时库存满则移除过剩原料腾位，历史满仓存档读档后可直接恢复出锭。
- **合金炉无法工作（历史囤积堵死输入）**：切换配方后，此前轮询炼钢囤积的煤/铜锭塞满库存，新配方原料进不来 → 合金炉把「不属于当前配方」以及「超过每种缓存上限」的物品吐出清库存。
- **合金炉单种原料囤满挤占另一种**（如钢锭炉煤囤满堵死铁锭、康铜炉铜锭囤满堵死镍锭）→ 为组装机/合金炉新增「每种原料缓存上限 128」（`ASSEMBLER_ITEM_CAP` / `ALLOY_FURNACE_ITEM_CAP`），三条入库路径（管道配送 / 机器拉料 / ME 出网导出）统一限流。

### 系统优化

- 组装机/合金炉的原料入库统一受「每物品缓存上限」约束，避免单种原料无限囤积、挤占其它原料，保证多配方稳定供料。

---

## Alpha v1.2.3 (2026-08-25)

### 新增功能

- **ME 接口输出过滤（锁定物品输出）**：右键 ME 接口打开过滤面板，点选物品锁定为白名单——该接口只把这些物品导出网络，其余物品留在网络内；过滤列表存入存档（`filter` 字段）。ME 存储单元/终端右键仍打开网络清单。
- **ME 网络 round-robin 均分导出**：同一网络内多个接口同时导出物品时，不再是"先到先得"，而是逐件轮询均分——接口级起点轮换 + 物品级按 ItemType 枚举序轮询，物品紧张时也能公平调度、不被单一接口/单一物品饿死。

### GUI 与贴图全面重设计（暗色工业风深化）

- **建筑贴图程序化重画并覆盖旧 Python 版 PNG**：炮塔（4 种 × 8 方向：八角底座 + 铆钉 + 旋转炮管）、采矿场（钻头 + 履带 + 等级标识 / 虚空光环）、储物桶（金属桶身 + 箍环）、组装机（齿轮）、旧版发电机、电线杆（横担 + 瓷绝缘子）。
- **机器贴图工业细节**：熔炉（铆钉 + 黄黑警告条 + 发光炉口）、合金炉（电极 + 发光核心）、电容库（电池单元组 + 指示灯）、燃煤发电机（涡轮叶片 + 排气栅栏 + 火焰），统一加倒角 + 渐变。
- **矿石 / 地形 / 敌人**：8 种矿石统一为「岩石基底 + 晶体矿团」；地形多层明暗斑 + 碎石 + 车辙；敌人改为机械机甲（履带 + 发光单眼 + 警告条纹 / 速度尖角）。
- **管道 / 分流器 / ME 设备渲染**：管道加双层金属外壳 + 圆形截面孔；分流器改为旋转分流盘（外环 + 叉臂 + 中心孔）；ME 设备加发光菱形、容量条 + 硬盘格栅、终端屏幕 + 键盘格。
- **GUI 精修**：顶部资源栏改为右对齐自适应 + 图标/名称/数量芯片式；侧边栏建筑按钮加类别色块；悬停提示动态高度 + 标题分隔线；商店/随身工作台/ME 面板头部统一彩色标题条。

### Bug修复

- **熔炉 / 合金炉只烧第一种矿石**：配方选择永远固定第一个"原料齐"的配方，导致只炼铁、其余矿饿死 → 改为配方轮询，均衡烧制各矿种。
- **矿物无法合成锭（锭被静默丢弃）**：熔炉原料与产物共用同一库存，机器拉料与 ME 出网导出两条路径把库存塞满，出炉时 `inv.add` 失败、锭被丢弃。修复：为正在冶炼的机器预留产物空间、封堵两条塞料路径、出锅时库存满则移除过剩原料腾位（历史存档读档后可直接恢复出锭）。
- **ME 接口过滤不生效**：物品进出机器时未检查 FaceConfig 面配置，从机器 OUTPUT 面也往里面灌原料，绕过过滤器 → 机器拉料与 ME 出网推送改为只走 INPUT 面，OUTPUT 面仅用于出产物（入网方向）。
- **ME 多物品无法轮询调度**：接口导出时物品选择顺序固定，靠前物品被抢、靠后饿死 → 新增物品级轮询指针。
- **资源栏数量恒显 0/1**：`std::unordered_map::count()` 返回"键是否存在"而非取值 → 改为 `playerInv[t]` 取实际数量。
- **移除了him**

### 系统优化

- MeSystem 出网导出重构为两阶段（入网吸入 + 出网轮询均分），`exportOne` / `deliverItemToNeighbor` 分离，过滤、面配置、产物预留统一收口到交付层。

---

## Alpha v1.2.1 (2026-08-17)

### 新增功能

- **AE2 式存储物流系统（后期科技）**——两段式物流完整落地：
  - **前期**：物品管道（EnderIO/Pipez 式，逐段运输，自动链接四邻）
  - **后期**：**ME 网络**（AE2 式"所有物品进网络"），三种新建筑：
    - **ME接口**：桥接器——从相邻管道/分流器**吸入物品入网**，向相邻机器/发电机/弹药塔/储物桶**导出物品出网**（输入总线+输出总线合体）
    - **ME存储单元**：为网络提供容量（每个 20000 件，多个叠加）
    - **ME终端**：右键查看全网物品清单（物品作为"数据"存于网络，全网共享、无实体搬运）
  - 设备四邻自动链接成网；机器输出（采矿机/熔炉/合金炉/组装机/储物桶）可直接入网，机器/发电机直接从网络取料
  - 网络编号按行主序 BFS 确定（存档可复现）；拓扑变化时物品按锚点归属，合并/分裂不丢物品
  - 网络存储随 JSON 存档完整保存（`me_networks`）

### Bug修复

- **存档致命 bug：读档中断导致大量方块/机器/物品丢失**（本版本最严重问题）:
  - 元凶：读档代码中 `bj.value("items", json::object()).begin() / .end()` 对**两个不同的临时 json 对象**取迭代器，迭代器悬空 → 只要熔炉/采矿机库存、ME 网络或玩家背包非空就抛异常，整个加载在建筑阶段中断，所有方块/物品丢失（共 4 处，全部修复为"先绑定引用再迭代"）
  - 连带修复：读档解析移到最后清空之前（存档损坏不再清掉当前游戏）；加载异常打印具体原因（stderr）
  - **存档内容全面补齐**：采矿机库存（已采未输出矿石）、所有建筑的面配置（采矿机/熔炉/合金炉/组装机/发电机/储物桶/电线——右键旋转过的面保留）、塔炮管朝向、储物桶输出计时、分流器轮询索引、摄像机位置、合金炉任务参数
  - 保存前自动刷新 ME 网络拓扑（刚放 ME 设备立即存档不丢网络物品）
  - 新增 `factory-td.exe --selftest-save` 存档往返自检（自动备份/恢复玩家存档，21 项逐项核对，退出码 0=通过）
- **管道物品卡在第一格无法传输**: 管道路由 BFS 此前只把储物桶/弹药塔当作接收端，机器（熔炉/合金炉/组装机/发电机）不是 → "矿机→管道→熔炉"线路中物品无处可交付而滞留。现已把机器/发电机纳入路由接收端（只收自己配方所需原料，合金炉断电不收）
- **管道/机器不认识 ME 存储单元与终端**: 管道路由与机器输出此前只认 MeInterface——现在**任意 ME 设备（接口/存储单元/终端）都是网络入口**：管道 BFS 可直接把物品送进 ME 网络，采矿机/熔炉/储物桶输出顶在 MeDrive/MeTerminal 上也能直接入网（AE2式"所有物品进网络"）
- **读档丢失管道中物品**: 管道同时存在于存档 `buildings` 与 `pipes` 两段，加载时 `pipes` 段重复 placeBuilding 因格子占用失败，缓冲静默丢失。现改为直接按坐标回填缓冲
- **5种新矿石显示纯紫块**: PNG 缺失时 `loadTexture` 先插入紫红回退贴图，导致程序化矿石生成被"已有贴图"判断跳过。新增 `tryLoadTexture`（失败不插回退），程序化矿石正常生成
- **测试版资源误报不足**: 无限资源只初始化了4种物品，铁锭/电路板/钢锭等新成本物品缺失导致"资源不足"。现无限模式下全部20种物品足额发放，且 `canAfford` 恒真、不扣费；读档后同样补全

### 系统优化

- 机器原料清单计算抽取为 `PipeSystem::wantedInputs`（管道拉取/ME导出/管道路由共用）
- ME 网络拓扑按脏标记延迟重建（放置/拆除时才重算）

### 存储&物流设计解析（add.txt ⑦ 补充：两段式物流）

**前期（管道，EnderIO/Pipez 式）**：物品是实体，逐段运输——矿机/机器把物品推进管道缓冲，管道每 0.25s BFS 找到最近可接收容器直接送达（AE2 只借鉴其"有路径即送达"的简化，不逐格滚动）。管道吞吐量有上限，逼着玩家进入下一阶段。
**后期（ME 网络，AE2 式）**：物品变为**数据**——ME接口把物品"数字化"存入网络（存储单元决定容量上限），任何联网设备都能访问全网库存；机器直接从网络拉配方原料，产物直接回网。物流瓶颈消失，玩家专注产线扩展。
**阶段门槛**：ME 设备造价要求电路板+钢锭（需合金炉 16EU/s 供电），自然把 ME 网络推向中后期。

---

## Alpha v1.2.0 (2026-08-16)

本版本按 `add.txt` 需求全面重做数值/存档/采矿/物流/合成/GUI，全部数据可热改。

### 新增功能

- **JSON 配置系统（改数值无需重编译）**: `assets/config.json` 运行时覆盖全部数值——金币/波次/矿点/矿机速率/塔/敌人/配方表/建筑成本/电网/管道/商店价目，缺失字段自动回退内置默认值
- **JSON 存档**: `saves/factory_td.json`（同 Python 版风格），覆盖矿点（含储量）/建筑（含采矿机等级、熔炉与合金炉任务、库存）/管道缓冲/分流器队列/敌人/全局状态
- **8 种矿石 + 有限矿点**: 铁/铜/煤/金/钻石/镍/银/铅，每种独立矿点随机散布全图（避开路径、互不重叠、种子 42），每矿点储量 1000，采尽消失
- **GT 式采矿场 4 档**:
  - 1/2/3 级采矿场：采集 **5×5 / 9×9 / 13×13** 范围内所有类型矿石，每秒产出 **4 / 16 / 256** 个（按矿点储量消耗）
  - **虚空采矿场**：无需矿点，每秒产出全类型矿石共 **4096** 个（8 种轮流）
- **电力单位改为每秒**: 全部发电/耗电/充放改为 EU/秒（发电机 32EU/s、电力塔 8EU/s、合金炉 16EU/s、电容库 64EU/s 充放）
- **传送带移除 → 物品管道**（参考 EnderIO/Pipez/AE2，详见下文解析）:
  - 取消动画与面配置九宫格，**自动链接四邻的容器/机器/管道**
  - **AE2 式即时路由**：每个转移周期 BFS 找最近可接收容器直接送达，无逐格移动；无路可送时物品停在管道缓冲（背压不消失）
  - 机器从相邻管道**主动拉取**所需原料（AE2 输入总线式），无关物品不会污染机器库存
  - 分流器重写：自动吸入/智能轮询均分——出口满时跳过，不再盲目轮流
- **数据驱动配方表（全部机器）**: 熔炉配方（6 矿石→锭）、合金炉配方、组装机配方、随身工作台配方全部在 `config.json` 中可增删改
- **合金炉**: 消耗 **16EU/秒** 电力 + 矿物锭生产合金（配方参考 GT/Mek/Thermal）:
  - 钢锭 = 2铁锭+2煤（GT 电弧炉/Mek 冶金注入/Thermal 感应炉通用配方）
  - 琥珀金锭 = 1金锭+1银锭 → 2（GT/Thermal）
  - 因瓦锭 = 2铁锭+1镍锭 → 3（GT/Thermal）
  - 康铜锭 = 1铜锭+1镍锭 → 2（Thermal）
- **随身工作台**（泰拉瑞亚/MC 式，**V 键**）: 前期物品手工合成迁移至此（电路板、弹药），组装机可对同配方自动量产
- **商店（B 键）**: 金币（击杀获得）+ 电路板 兑换 矿石/合金/机器；价目表在 `config.json` 的 `shop_offers`（当前为占位起步价目，正式配方表以后再写）；兑换机器直接获得一次免费放置机会
- **分键召唤敌人**: **Z** 普通 / **X** 快速 / **C** 坦克（U 保留兼容）
- **打包中文字体**: `assets/fonts/simsun.ttc` 随游戏分发，优先加载，不依赖系统字体
- **GUI 与贴图重写（暗色工业风）**:
  - 全 UI 暗色主题（深灰面板 + 橙色强调，Factorio 式）
  - 地形程序化重写（草地草斑/路径碎石+车辙+压暗边沿）
  - **Xaero 式小地图**（左下角 160px 方块：地形/矿点/建筑/敌人 + 视野框，0.3s 节流重建）
  - **世界地图（M 键）**: Xaero's World Map 式全屏总览（1 格=1 像素，最近邻放大，含矿点/敌人/视野框）

### Bug修复

- 采矿机不再要求脚下有矿点（改为范围内采集），右键旋转覆盖全部 4 档采矿场
- 分流器面配置与管道九宫格全部取消（自动链接），面编辑器仅保留电线/机器/桶
- 熔炉"只炼铁铜"写死逻辑 → 数据驱动配方表统一处理
- 组装机配方数组越界风险（存档旧配方索引）增加边界保护
- 旧存档兼容：建筑类型枚举新成员（合金炉）追加在末尾，旧索引不变

### 系统优化

- 管道路由 BFS 使用"版本戳"visited 数组（零分配）；分流器/拉取按节流间隔运行
- 小地图/世界地图渲染节流 0.3s，只重建不逐帧绘制
- 存档加载顺序：清空 → 矿点 → 建筑 → 管道 → 敌人 → 状态

### 物品管道设计解析（add.txt ⑦ 要求的思路说明，ME 网络阶段见 v1.2.1）

**为什么这样设计**（参考 EnderIO/Pipez/AE2 三模组的取舍）：

1. **连接模型 = EnderIO 导管/Pipez**：管道是 1×1 方块，自动连接相邻的容器（储物桶）、机器（熔炉/合金炉/组装机/发电机）与其他管道，没有面配置九宫格——玩家只管把管道铺过去，连接自动完成。
2. **传输模型 = AE2 电缆网络**：AE2 不把物品做成逐格滚动的实体，而是"网络有路径就送达"。本作同样取消动画：每 0.25s 对管道缓冲做一次 BFS，找到最近的可接收容器（桶/弹药塔）直接传送；找不到就留在管道缓冲（背压），不堵不丢。
3. **输入模型 = AE2 输入总线**：机器不被动接货，而是从相邻管道**拉取**自己配方需要的原料（熔炉拉矿、合金炉拉锭、组装机拉配方原料、发电机拉煤），无关物品永远不会进入机器库存。
4. **分流器 = Pipez 的高级分配**：作为网络节点直通，自己队列里的物品按"轮询+跳过满出口"均分给管道/桶/塔。
5. **吞吐量分层**：机器→管道每次 1 件、管道路由每周期 4 件/段、机器→桶直连整批转移；高速矿机（3级/虚空）应直接接桶或用多路管道，避免物流瓶颈。
   每个物品的合成表：矿石由矿机采集、锭由熔炉配方表冶炼、合金由合金炉配方表合成、电路板/弹药由随身工作台或组装机合成（全部数据驱动）。

---

## Alpha v1.1.0 (2026-08-15)

### 新增功能

- **C++ 完整重构版**: 用 C++20 + SFML 2.6 + EnTT 将 Python/Pygame 版整体重构移植
  - 地图扩展为 **200×200 网格**（敌人路径与矿点范围按原形状 ×4 等比缩放）
  - **ECS 架构**: 8 个系统 + 11 个组件，EnTT view 批量查询（SoA 布局）
  - 炮塔 8 方向、机器 4 方向、敌人、矿石、地形贴图全部迁移
- **熔炉与冶炼链**: 铁矿石→铁锭、铜矿石→铜锭（各 2.0s）；工业风 4 方向贴图（含输出方向箭头）；放置时弹出方向选择
- **组装机多配方系统**（替代弹药制造机）:
  - 数据驱动配方表 `ASSEMBLER_RECIPES`（新增配方只需追加一行）
  - 右键组装机弹出配方选择菜单，选定后只按该配方合成，可重复选择
  - 当前配方: 弹药（2铁锭+1铜锭→1弹药）、电路板（1铁锭+1铜锭→1电路板，用途预留）
- **格雷科技式电网**（不可无限）:
  - 四方向面配置 NONE/INPUT/TRANSFER/OUTPUT，BFS 定向路由
  - 燃煤发电机（32EU/帧，煤燃 5s）、旧版大功率发电机（1煤→3000EU/3s）、电容库（50000EU/64EU 充放）、电线杆（150px 恒导通）、电力塔（8EU/帧）
  - 移除无限燃料测试模式，发电机必须由传送带供煤
- **面配置编辑器升级**: 输入箭头朝内、输出箭头朝外；中文模式标签（无/输入/输出/传输）；颜色图例（灰=无、绿=输入、红=输出）
- **矿石全图随机混布**: 三种矿随机散布全图（铁128/铜96/煤80），避开路径、互不重叠，种子 42 可复现
- **测试模式刷怪**: 波次自动生成开关（`WAVE_AUTO_SPAWN`），按 **Z 键**生成普通敌人（U 键保留）
- **存档系统**: F5 保存 / F9 加载，自定义文本格式（含面配置、传送带物品、组装机配方）
- **建筑交互**: 方向选择悬浮窗（4/8 向）、传送带单点/两点式放置、DEL 拆除返还材料

### Bug修复

- **贴图占 2×2 格、画面异常**: 全部 PNG 实为 64×64/90×90 而渲染按 32px 处理 → 渲染全面分辨率无关化
- **窗口最大化后点击失效**: 坐标变换与 UI 布局写死 1280×720 → 改为运行时窗口尺寸（Resized 事件 + updateLayout）
- **面编辑器四箭头全朝外、无法区分输入输出**: 输入箭头朝内 + 中文标签 + 图例
- **拆除传送带物品掉落地面且无法消除**: 拆除时同步清除物品 + 渲染防御检查
- **面配置三态循环到不了输出**: NONE→INPUT→OUTPUT 专用循环（跳过 TRANSFER）
- **敌人悬停血条永不显示**: 补敌人命中检测
- **地形双重绘制**: 草地/路径分遍绘制
- **按钮文本存在无用 [数字] 前缀**: 移除
- **同种矿石连片生成**: 改为全图随机混布
- **移除了him**
- 编译期修复: `sf::Color` 非字面量、`fromUtf8` 重载、EnTT 3.13 view API 变更、结构化绑定数量、负坐标取整、窗口标题乱码等 8 类

### 系统优化

- **性能**: 目标 200×200 网格+数万物品 60FPS
  - 传送带三阶段并行更新（`std::execution::par`，源格/目标格不变量保证无竞争）
  - 炮塔索敌 SpatialGrid 空间哈希（塔间并行）
  - 地形/传送带/物品 `sf::VertexArray` 批量渲染 + 视野剔除
  - Tracy 打点封装（`TRACY_ENABLE` 可选启用）
- **构建**: CMake 三级依赖探测（本地 deps → 系统已装 → 联网 FetchContent）、`build.bat` 一键构建（自动找小熊猫 g++/cmake、自动检测 Clash 代理）、TBB 缺失自动降级串行
- **离线依赖**: `factory-td/deps/local2/` 预置 SFML 2.6.1（mingw 预编译）+ EnTT v3.13.2 头文件，可完全离线构建
- **UI**: 面板贴窗口右缘、窗口缩放/最大化自适应

---

# 以下为 Python 版（python版（老版）/）历史更新日志

## Alpha v1.0.4 (历史版本 - 2026-07-19)

### 新增功能

- **工业电力系统完整实现**:

  - **燃煤发电机**: 32 EU/t 持续输出，无限燃料模式
  - **电容库**: 50000 EU 容量，64 EU/t 充/放速率，电网缓冲
  - **电线**: 4方向独立面配置系统
    - 每面 4 种模式：NONE（断开）→ INPUT（输入）→ TRANSFER（传输）→ OUTPUT（输出）
    - 右键点击电线打开面配置编辑器，点击方向按钮循环切换面模式
    - 仅绘制有连接方向的线缆段，未连接方向不显示
    - 面颜色标识：灰（NONE）/ 蓝（INPUT）/ 黄（TRANSFER）/ 橙（OUTPUT）
  - **电力塔**: 8 EU/t 消耗，从电网获取电力，供电时显示"供电中"
  - **BFS电力路由算法**: 基于电线面配置匹配的定向传播
  - **UI面板**: 右侧面板显示发电量、消耗量、电容状态、网络状态
- **电力快捷键**:

  - 0: 选择燃煤发电机
  - -: 选择电容库
  - =: 选择电线
- **电力存档支持**: 电线面配置（faces）存入存档

### Bug修复

- **电塔不进电**: 修复 `power_manager.update()` 在发电机 `update()` 之前执行的顺序问题
- **电塔 `current_power` 为 0**: `set_powered(True)` 时自动填满 `current_power = max_power`
- **电线连接使用了错误的对象类型**: 统一使用 PowerTower 包装器而非原始 Tower 对象
- **存档加载 `'NoneType' has no attribute 'add_item'`**: 电力塔 `inventory` 为 None 的防护检查
- **`_rebuild_power_wire_appearances` 方法别名错误**: 修复为独立的方法
- **电容库放置报错**: 修复 `connected_objects` 属性缺失检查
- 移除了him

### 系统优化

- 电线外观根据 4 方向连接状态动态刷新
- 发电机实体更新顺序调整，确保电力状态正确
- 电容库和电力塔放置后自动扫描相邻电线建立连接

---

## Alpha v1.0.3 (2026-06-25)

### 新增功能

- **存档系统**: 完整的存档/加载功能

  - 快捷键: F5保存, F9加载
  - UI按钮: 右侧面板保存/加载按钮
  - 保存内容: 所有建筑、物品、矿石资源、敌人、游戏状态
  - 存档位置: `python版（老版）/saves/save.json`
- **敌人系统恢复**: 按U键手动生成敌人

  - 敌人沿预设路径行走
  - 到达终点扣生命值
  - 被塔击杀获得金币奖励
- **敌人贴图**: pygame生成的工业风格贴图

  - 基础敌人: 红色圆形，愤怒表情
  - 快速敌人: 橙色菱形，尖锐外形
  - 重型敌人: 深红六边形，装甲纹理
- **塔弹药补给系统**

  - 塔初始弹药改为0，需要通过传送带供给
  - 传送带可将弹药输送到塔
  - 塔悬浮窗显示弹药数量
- **储物桶输出功能**

  - 添加输出计时器（每0.5秒输出一个物品）
  - 按设定方向输出到前方传送带

### Bug修复

- **存档保存不完整**: 修复塔、采矿机、传送带物品未保存的问题
- **塔类型加载错误**: 加载时添加`_tower`后缀匹配
- **采矿机加载失败**: 矿石恢复顺序调整为先恢复矿石，再恢复采矿机
- **叠加放置建筑**: 添加位置占用检查，防止在同一位置放置多个建筑
- **传送带未输送到塔**: 添加塔检测和弹药接收逻辑
- **TILE_SIZE未定义**: save_system.py导入settings.TILE_SIZE
- 移除了him

### 系统优化

- 塔悬浮窗高度从100调整为140，显示更清晰
- `_place_building`改为返回实体对象，便于存档恢复时设置属性

---

## Alpha v1.0.2

### 新增功能

- **机器悬浮窗**: 鼠标悬停在机器上显示详细信息

  - 采矿机: 矿石类型、工作状态、进度百分比
  - 弹药制造机: 配方信息、原材料库存、弹药数量
  - 发电机: 燃料数量、电力输出
  - 储物桶: 存储物品汇总
- **储物桶系统**

  - 无限容量 (99999)
  - 显示"桶"字标识
  - 物品数量显示
  - 可从任意方向接收物品
- **传送带输入系统**

  - 传送带物品自动传入前方机器/储物桶
  - 支持弹药制造机、发电机、储物桶、箱子
- **弹药制造机**: 移除电力需求，改为自动合成的大箱子模式

  - 每秒检测一次配方
  - 配方: 2铁矿石 + 1铜矿石 → 1弹药
  - 自动输出弹药到前方传送带

### Bug修复

- **传送带物品传递**: 修复物品在 blocks_transfer=True 时被错误清空的问题
- **弹药制造机输出**: 修复原材料被错误输出到传送带的问题（现在只输出弹药）
- **储物桶放置**: 修复储物桶无法正确放置的问题
- **悬浮窗文字颜色**: 改为深色文字，提高可读性
- **字体加载**: 优化中文字体加载逻辑
- 移除了him

### 系统优化

- ui优化，提高用户体验

---

## Alpha v1.0.1

### 新增功能

- 采矿机系统框架
  - 矿石类型自动选择

### Bug修复

- 修复了已知问题
- 移除了him

---

## Alpha v1.0.0

### 初始版本

- 基础游戏框架
- 资源采集系统框架
- 传送带物流系统框架
- 电力网络系统框架
- 塔防战斗系统框架
- 敌人波次系统框架
