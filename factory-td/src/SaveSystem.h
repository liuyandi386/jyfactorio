#pragma once
// =====================================================================
// SaveSystem.h —— 存档系统（F5保存 / F9读档）
//
// 移植 save_system.py：保存游戏状态、建筑（含库存/面配置/传送带物品）、
// 敌人与波次状态。由于依赖清单仅限 sfml/entt，采用自定义文本格式
// （无需第三方JSON库）。
// =====================================================================
class Game;

/// 保存游戏（返回是否成功）
bool saveGame(Game& g);
/// 读取游戏（返回是否成功）
bool loadGame(Game& g);
/// 是否存在存档
bool hasSave();
